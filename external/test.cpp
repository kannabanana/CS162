#include <iostream>
#include <fstream>
#include <cctype>
#include <cstring>

struct count
{
int num[20];

}
void get_info(count * dracula,int i); 
void write_out(count * dracula, int i);
void read_in(count * dracula, int i);
void display(count * dracula, int i);

using namespace std;
int main()
{
count dracula[200];
int total_classes = 0;
do
{
	ifstream file_out;
	file_in.open("external_test.text",ios::app);
	if(file_in);
	{
		while(!file_in.eof())
		{
			getline(file_in,
		}
		myfile_close();
	}	
//	get_info(dracula,i);
}
while (i <=60);

return 0;

}
